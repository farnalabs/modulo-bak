# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.env
.env.*
*.egg-info/
dist/
build/
.eggs/

# uv — lockfile IS committed (reproducible installs)
# uv.lock

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Generated log files (not to be committed)
/backend/backend-err.log
/backend/backend-out.log

# pytest
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
test-results/
.hypothesis/

# ruff
.ruff_cache/

# Alembic
# (migrations tracked in git — do not ignore)

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/.cache/
*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Docker
docker-compose.override.yml

# Secrets (never commit these)
*.pem
*.key
secrets/

# Mutation testing reports (generated by Stryker Mutator)
reports/mutation/
.stryker-tmp/
stryker.log

# Local agent delivery state and helpers (intentionally machine-local)
.agent-work/

# OpenCode
.opencode/cache/
.opencode/*.session

# Agent worktrees (created by improve-codebase / qa-iterate)
.agents/worktrees/

# Local — machine-local tooling, scripts, resources.
local/
.local/
.tool-cache/

# UX review capture
ux-review-capture.json
ux-review-report.md
# QA findings artifacts (generated by multi-lens QA, never committed)
findings.json
__lens*-findings.json
qa-findings-lens*.json

# Local SQLite databases created by the default development and test settings
**/modulo.db
**/test.db
/backend/*.db
/backend/*.sqlite
/backend/*.sqlite3

# Vite runtime logs (generated by frontend dev servers)
.vite*.log

# Stray root lockfile (root package.json was removed in #672; only frontend/
# has a package.json). Prevents accidental `npm i` at root from committing a
# spurious lockfile that shadows frontend/package-lock.json.
/package-lock.json

# The evidence FILE (alpha-exit-evidence.json) IS committed — it is the template.
